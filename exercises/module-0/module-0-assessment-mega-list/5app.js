// Create a button in HTML. 
// Bring that button into Javascript. 
// Add an event listener to that button
// // using JS techniques so that when 
// the button is clicked it executes a for loop.

function myFunction() {
    document.getElementById("demo").innerHTML = "HOLA MUNDO";
}